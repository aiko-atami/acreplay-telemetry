{
  "name": "acreplay-telemetry",
  "private": true,
  "scripts": {
    "changeset": "changeset",
    "bump": "changeset version",
    "build": "pnpm --filter './packages/**' run build",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@typescript/native-preview": "catalog:"
  },
  "packageManager": "pnpm@10.33.0"
}
